{
  "name": "database",
  "version": "0.0.0",
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate && prisma db push --schema prisma/cache.prisma",
    "db:push": "prisma db push --skip-generate",
    "drizzle:introspect": "dotenv drizzle-kit introspect:mysql"
  },
  "dependencies": {
    "@planetscale/database": "^1.7.0",
    "@prisma/client": "4.16.0",
    "drizzle-orm": "^0.27.0",
    "zod": "^3.21.4"
  },
  "devDependencies": {
    "dotenv-cli": "^7.2.1",
    "drizzle-kit": "^0.19.1",
    "prisma": "4.16.0"
  }
}
